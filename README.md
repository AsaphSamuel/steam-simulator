Olá, eu sou o Samuel Asaph. Para o funcionamento do programa é necessário seguir algumas diretrizes a seguir:

Versões atualizadas do Visual Studio Code.
Instalar o Python 3.13+.
Instalar o Flask (comando 'pip install flask' através do terminal do python).
Instalar o SQLAlchemy (comando 'pip install sqlalchemy' através do terminal do python).
